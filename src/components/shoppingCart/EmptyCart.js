import React, { Fragment } from 'react'
import { Typography } from './mui/emptyCartMui'

const EmptyCart = () => (
  <Fragment>
    <Typography>
      There are no items in your cart.
    </Typography>
  </Fragment>
)

export default EmptyCart